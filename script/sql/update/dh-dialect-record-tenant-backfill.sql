-- ============================================================
-- 方言采集记录租户回填脚本
-- 将历史匿名提交且 tenant_id 为空的数据回填到默认租户
-- ============================================================

UPDATE `dh_dialect_record`
SET `tenant_id` = '000000'
WHERE `tenant_id` IS NULL;
