-- ============================================================
-- 方言采集匿名提交 DDL 变更脚本
-- 允许 C 端未登录提交方言录音，匿名记录 user_id 为空
-- ============================================================

ALTER TABLE `dh_dialect_record`
    MODIFY COLUMN `user_id` BIGINT(20) NULL COMMENT '提交用户ID（匿名提交为空）';
